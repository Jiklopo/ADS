#include <iostream>
using namespace std;

int main(){
    int n, x, y;
    cin >> n >> x >> y;
    int time = min(x, y);
    n--;
    while(n > 0)
    {
        
    }
    cout << time;
    return 0;
}